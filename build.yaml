---
include:
  # ---- Dongle / central (Prospector display) ----
  # Prospector "main" branch targets ZMK v0.3, so the XIAO board id is
  # seeeduino_xiao_ble.
  # KVM-clean variant: the studio-rpc-usb-uart snippet is intentionally removed
  # so the dongle enumerates as a plain USB HID keyboard (not a composite
  # HID+serial device) — composite USB is what confuses KVM switches. USB HID is
  # kept alive via CONFIG_ZMK_USB in the dongle Kconfig.defconfig. No Studio here.
  - board: seeeduino_xiao_ble
    shield: corne_dongle prospector_adapter

  # ---- Left half, now a PERIPHERAL ----
  - board: nice_nano_v2
    shield: corne_left nice_view_adapter nice_view
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

  # ---- Right half, now a PERIPHERAL ----
  - board: nice_nano_v2
    shield: corne_right nice_view_adapter nice_view
    cmake-args: -DCONFIG_ZMK_SPLIT=y -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

  # ---- settings_reset for every board you flash ----
  - board: nice_nano_v2
    shield: settings_reset
  - board: seeeduino_xiao_ble
    shield: settings_reset
